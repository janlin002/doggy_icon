import { Size } from '../types';

type Props = {
    icon: string;
    style?: any;
    size?: Size;
};
declare const DoggyIcon: ({ icon, style, size }: Props) => import("react/jsx-runtime").JSX.Element;
export default DoggyIcon;
